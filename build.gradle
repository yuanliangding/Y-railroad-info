apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
	mavenCentral()
	jcenter()
}

jar {
	manifest {
		attributes ('Main-Class': 'yuanliangding.interview.YRailroadInfo.Application',"Implementation-Title": "Y-Railroad")
	}
	from {
		configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
	}
	exclude 'META-INF/*.RSA', 'META-INF/*.SF','META-INF/*.DSA'
}

dependencies {
	testCompile group: 'junit', name: 'junit', version: '4.12'
}
